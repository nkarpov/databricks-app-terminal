bundle:
  name: databricks-app-terminal

resources:
  apps:
    app:
      name: databricks-app-terminal
      description: "Databricks App Terminal: multi-session web terminal"
      source_code_path: ./

targets:
  default:
    default: true
